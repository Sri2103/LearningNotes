# Distributed caching

- In distributed cache, the cache itself is a seperate system that runs on multiple servers that works together in a network

- data is partitoned and replicated across the network

## Types of distributed caching

```mermaid
mindmap
    distributed -caching
        distributed key value store
        cdn
```

![alt text](./files//distributed-caching.webp)

## cdn cache

- stores static assets close to users on edge servers

## distributed key-value store

- stores cached data across the servers for the data to see
