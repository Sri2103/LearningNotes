# web caching

- Temporarily saving various data between user and original server,such as in browser, proxy-server or in a cdn

## Browser cache

- stores the static assets in users browser cache
- uses cache control headers in http headers

## Proxy Cache

- stores http response between client and server , example varnish caching and squid proxy

## Server side Cache

- stores the generated html to avoid repeated generation.
