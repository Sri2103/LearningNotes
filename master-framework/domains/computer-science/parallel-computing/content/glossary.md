Mutex → mutual exclusion lock  
Atomic operation → indivisible CPU-level update  
CAS → compare and swap  
False sharing → threads modifying same cache line  
Happens-before → ordering guarantee in memory model  
Wait-free → every thread makes progress  
Goroutine → lightweight concurrent function in Go  
Work stealing → idle workers steal tasks
