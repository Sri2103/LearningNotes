# Amdahl’s Law
Speedup = 1 / ( (1 - P) + (P/N) )

# Worker Pool Template (Go)
jobs → workers → results

# Deadlock Detection Template
list locks → check cyclic dependency

# Parallel Divide & Conquer Template
parallel(left)  
parallel(right)  
combine  
