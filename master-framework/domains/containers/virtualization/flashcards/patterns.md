### Sidecar Pattern

Auxiliary container attached to main container.

### Init Pattern

Short-lived container initializing environment.

### Distroless Pattern

Minimal runtime-only filesystem.

### Multi-stage Build Pattern

Build in one stage → copy minimal artifacts to final stage.
