ACID → atomicity, consistency, isolation, durability  
MVCC → multi-version concurrency control  
WAL → write-ahead log  
LSM → log-structured merge tree  
Quorum → required nodes for consistency  
Hot partition → uneven shard load
