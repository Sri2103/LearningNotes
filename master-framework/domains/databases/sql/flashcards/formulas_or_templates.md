# Quorum Formula (Dynamo-style)

R + W > N

# Index Selection Template

WHERE column → single index  
WHERE A AND B → composite index  
ORDER BY → clustered index

# Normalization Template

1NF → atomic columns  
2NF → full primary key dependency  
3NF → no transitive dependency
