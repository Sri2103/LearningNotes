# DevOps/SRE Trigger Checklist

- High error rate → check deployments
- Slow service → check latency graphs
- Restart loops → check probes
- High cost → check infra provisioning
- High CPU → check autoscaling
- High memory → check leaks
- 5xx errors → check dependencies
- Alerts firing → check SLIs/SLOs
