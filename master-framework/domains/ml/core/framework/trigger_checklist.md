# ML/DL Trigger Checklist

- accuracy too high → data leakage suspicious
- loss stuck → bad learning rate
- divergence → exploding gradients
- high variance → too complex model
- high bias → increase model size or epochs
- inference slow → quantization/pruning needed
- drift → retraining required
