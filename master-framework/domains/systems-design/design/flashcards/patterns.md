### Read-Heavy System Pattern

Cache → DB → Persistent store

### Write-Heavy System Pattern

Log-based storage → async processors → DB

### Event-Driven Pattern

Producer → Queue → Consumer

### Microservices Pattern

API Gateway → Services → Shared infra
