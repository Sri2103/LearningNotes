### Component Composition Pattern

Combine smaller components to build bigger UI.

### Controller-Service-Repository Pattern

Separation of concerns in backend.

### Cache-Aside Pattern

Check cache → if missing → load DB → store in cache.

### Pagination Pattern

Limit + Offset or Cursor-based.
